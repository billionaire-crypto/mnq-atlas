# Unit O ratification profiles

Each `*.json` file is one immutable canonical
`unit_o_ratification_profile_v1` record. A profile adds pins for a new
versioned Unit O artifact without editing or weakening the historical v1
ratification constants. Corrections are new entries; existing bytes are never
edited or deleted.
