# Phase 10 Cost-Only Benchmark Authorization

Date: 2026-08-14

Codex question (verbatim):

> Do you authorize me to run the Phase 10 cost-only benchmark against the real 900-session corpus in Stages A and B as scoped—executing real replications at reduced permutation counts, while every real p-value, statistic, coherence value, region, localization overlap, rejection event, contrast, quantile, and per-cell output is destroyed unread in a `finally` cleanup outside the repository—and then prepare but not execute the Stage C rental procedure? This is not the calibration run; no 300-replication run or B=4999 execution will occur.

Operator answer (verbatim):

> YES
