"""Calendar-isolated Phase 7 return and scale computations.

Every concrete module in this package exposes ``calendar_isolation_probe`` so
the pre-authorized A5 gate can discover and execute the package without a
hard-coded module inventory.
"""

